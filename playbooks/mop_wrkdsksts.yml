// Assisted by watsonx Code Assistant
// Code generated by WCA@IBM in this programming language is not approved for use in IBM product development.

---
- name: Get disk statistics on IBM i
  hosts: hosts
  gather_facts: no
  collections:
    - ibm.ibm_i
  tasks:
    - name: Gather disk statistics
      ibm_i_command:
        commands:
          - db2 "connect to your_database_name user your_username using your_password"
          - "SELECT * FROM TABLE (QSYS2.SYSDISKSTAT(RESET_STATISTICS => 'NO'))"
          - db2 "terminate"
        

---

