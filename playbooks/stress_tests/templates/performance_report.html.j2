<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Rapport de Performance IBM i - {{ hostname }}</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            line-height: 1.6;
            color: #333;
            background: #f5f5f5;
            padding: 20px;
        }
        
        .container {
            max-width: 1200px;
            margin: 0 auto;
            background: white;
            padding: 40px;
            box-shadow: 0 0 20px rgba(0,0,0,0.1);
            border-radius: 8px;
        }
        
        .header {
            text-align: center;
            padding-bottom: 30px;
            border-bottom: 3px solid #0066cc;
            margin-bottom: 40px;
        }
        
        .header h1 {
            color: #0066cc;
            font-size: 2.5em;
            margin-bottom: 10px;
        }
        
        .header .subtitle {
            color: #666;
            font-size: 1.2em;
        }
        
        .metadata {
            background: #f8f9fa;
            padding: 20px;
            border-radius: 5px;
            margin-bottom: 30px;
        }
        
        .metadata-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 15px;
        }
        
        .metadata-item {
            padding: 10px;
        }
        
        .metadata-item strong {
            color: #0066cc;
            display: block;
            margin-bottom: 5px;
        }
        
        .status-badge {
            display: inline-block;
            padding: 5px 15px;
            border-radius: 20px;
            font-weight: bold;
            font-size: 0.9em;
        }
        
        .status-ok {
            background: #28a745;
            color: white;
        }
        
        .status-warning {
            background: #ffc107;
            color: #333;
        }
        
        .status-critical {
            background: #dc3545;
            color: white;
        }
        
        .section {
            margin-bottom: 40px;
        }
        
        .section-title {
            color: #0066cc;
            font-size: 1.8em;
            margin-bottom: 20px;
            padding-bottom: 10px;
            border-bottom: 2px solid #e0e0e0;
        }
        
        .metrics-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 20px;
            margin-bottom: 30px;
        }
        
        .metric-card {
            background: #f8f9fa;
            padding: 20px;
            border-radius: 8px;
            border-left: 4px solid #0066cc;
        }
        
        .metric-card h3 {
            color: #333;
            margin-bottom: 15px;
            font-size: 1.2em;
        }
        
        .metric-row {
            display: flex;
            justify-content: space-between;
            padding: 8px 0;
            border-bottom: 1px solid #e0e0e0;
        }
        
        .metric-row:last-child {
            border-bottom: none;
        }
        
        .metric-label {
            color: #666;
            font-weight: 500;
        }
        
        .metric-value {
            font-weight: bold;
            color: #333;
        }
        
        .change-positive {
            color: #28a745;
        }
        
        .change-negative {
            color: #dc3545;
        }
        
        .change-neutral {
            color: #666;
        }
        
        .comparison-table {
            width: 100%;
            border-collapse: collapse;
            margin: 20px 0;
            background: white;
        }
        
        .comparison-table th {
            background: #0066cc;
            color: white;
            padding: 12px;
            text-align: left;
            font-weight: 600;
        }
        
        .comparison-table td {
            padding: 12px;
            border-bottom: 1px solid #e0e0e0;
        }
        
        .comparison-table tr:hover {
            background: #f8f9fa;
        }
        
        .recommendations {
            background: #fff3cd;
            border-left: 4px solid #ffc107;
            padding: 20px;
            border-radius: 5px;
            margin: 20px 0;
        }
        
        .recommendations h3 {
            color: #856404;
            margin-bottom: 15px;
        }
        
        .recommendations ul {
            list-style-position: inside;
            color: #856404;
        }
        
        .recommendations li {
            padding: 5px 0;
        }
        
        .chart-container {
            margin: 30px 0;
            padding: 20px;
            background: #f8f9fa;
            border-radius: 8px;
        }
        
        .progress-bar {
            width: 100%;
            height: 30px;
            background: #e0e0e0;
            border-radius: 15px;
            overflow: hidden;
            margin: 10px 0;
        }
        
        .progress-fill {
            height: 100%;
            background: linear-gradient(90deg, #0066cc, #0099ff);
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-weight: bold;
            transition: width 0.3s ease;
        }
        
        .footer {
            text-align: center;
            padding-top: 30px;
            margin-top: 40px;
            border-top: 2px solid #e0e0e0;
            color: #666;
        }
        
        @media print {
            body {
                background: white;
                padding: 0;
            }
            
            .container {
                box-shadow: none;
                padding: 20px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Header -->
        <div class="header">
            <h1>📊 Rapport de Performance IBM i</h1>
            <div class="subtitle">Comparaison Avant/Après Mise à Jour</div>
        </div>

        <!-- Metadata -->
        <div class="metadata">
            <div class="metadata-grid">
                <div class="metadata-item">
                    <strong>Serveur</strong>
                    {{ hostname }}
                </div>
                <div class="metadata-item">
                    <strong>Date du Rapport</strong>
                    {{ report_date }}
                </div>
                <div class="metadata-item">
                    <strong>Baseline</strong>
                    {{ baseline_date }}
                </div>
                <div class="metadata-item">
                    <strong>Validation</strong>
                    {{ validation_date }}
                </div>
                <div class="metadata-item">
                    <strong>Statut Global</strong>
                    <span class="status-badge status-{{ overall_status | lower }}">{{ overall_status }}</span>
                </div>
            </div>
        </div>

        <!-- Executive Summary -->
        <div class="section">
            <h2 class="section-title">Résumé Exécutif</h2>
            <div class="metrics-grid">
                <div class="metric-card">
                    <h3>🖥️ CPU</h3>
                    <div class="metric-row">
                        <span class="metric-label">Baseline</span>
                        <span class="metric-value">{{ cpu_baseline }}%</span>
                    </div>
                    <div class="metric-row">
                        <span class="metric-label">Validation</span>
                        <span class="metric-value">{{ cpu_validation }}%</span>
                    </div>
                    <div class="metric-row">
                        <span class="metric-label">Changement</span>
                        <span class="metric-value change-{{ 'negative' if cpu_change > 0 else 'positive' }}">
                            {{ '+' if cpu_change > 0 else '' }}{{ cpu_change }}% ({{ cpu_change_percent }}%)
                        </span>
                    </div>
                    <div class="metric-row">
                        <span class="metric-label">Statut</span>
                        <span class="status-badge status-{{ cpu_status | lower }}">{{ cpu_status }}</span>
                    </div>
                </div>

                <div class="metric-card">
                    <h3>💾 Mémoire</h3>
                    <div class="metric-row">
                        <span class="metric-label">Baseline</span>
                        <span class="metric-value">{{ memory_baseline }}%</span>
                    </div>
                    <div class="metric-row">
                        <span class="metric-label">Validation</span>
                        <span class="metric-value">{{ memory_validation }}%</span>
                    </div>
                    <div class="metric-row">
                        <span class="metric-label">Changement</span>
                        <span class="metric-value change-{{ 'negative' if memory_change > 0 else 'positive' }}">
                            {{ '+' if memory_change > 0 else '' }}{{ memory_change }}% ({{ memory_change_percent }}%)
                        </span>
                    </div>
                    <div class="metric-row">
                        <span class="metric-label">Statut</span>
                        <span class="status-badge status-{{ memory_status | lower }}">{{ memory_status }}</span>
                    </div>
                </div>

                <div class="metric-card">
                    <h3>💿 Disque</h3>
                    <div class="metric-row">
                        <span class="metric-label">Baseline</span>
                        <span class="metric-value">{{ disk_baseline }}%</span>
                    </div>
                    <div class="metric-row">
                        <span class="metric-label">Validation</span>
                        <span class="metric-value">{{ disk_validation }}%</span>
                    </div>
                    <div class="metric-row">
                        <span class="metric-label">Changement</span>
                        <span class="metric-value change-{{ 'negative' if disk_change > 0 else 'positive' }}">
                            {{ '+' if disk_change > 0 else '' }}{{ disk_change }}%
                        </span>
                    </div>
                </div>
            </div>
        </div>

        <!-- Detailed Comparison -->
        <div class="section">
            <h2 class="section-title">Comparaison Détaillée</h2>
            
            <h3>CPU Performance</h3>
            <table class="comparison-table">
                <thead>
                    <tr>
                        <th>Métrique</th>
                        <th>Baseline</th>
                        <th>Validation</th>
                        <th>Changement</th>
                        <th>% Changement</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td>Utilisation CPU</td>
                        <td>{{ cpu_baseline }}%</td>
                        <td>{{ cpu_validation }}%</td>
                        <td class="change-{{ 'negative' if cpu_change > 0 else 'positive' }}">
                            {{ '+' if cpu_change > 0 else '' }}{{ cpu_change }}%
                        </td>
                        <td class="change-{{ 'negative' if cpu_change_percent > 0 else 'positive' }}">
                            {{ '+' if cpu_change_percent > 0 else '' }}{{ cpu_change_percent }}%
                        </td>
                    </tr>
                    <tr>
                        <td>Load Average (1 min)</td>
                        <td>{{ load_avg_baseline }}</td>
                        <td>{{ load_avg_validation }}</td>
                        <td class="change-{{ 'negative' if load_avg_change > 0 else 'positive' }}">
                            {{ '+' if load_avg_change > 0 else '' }}{{ load_avg_change }}
                        </td>
                        <td>-</td>
                    </tr>
                </tbody>
            </table>

            <h3 style="margin-top: 30px;">Mémoire</h3>
            <table class="comparison-table">
                <thead>
                    <tr>
                        <th>Métrique</th>
                        <th>Baseline</th>
                        <th>Validation</th>
                        <th>Changement</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td>Utilisation Mémoire</td>
                        <td>{{ memory_baseline }}%</td>
                        <td>{{ memory_validation }}%</td>
                        <td class="change-{{ 'negative' if memory_change > 0 else 'positive' }}">
                            {{ '+' if memory_change > 0 else '' }}{{ memory_change }}%
                        </td>
                    </tr>
                    <tr>
                        <td>Mémoire Utilisée</td>
                        <td>{{ memory_used_baseline }} GB</td>
                        <td>{{ memory_used_validation }} GB</td>
                        <td class="change-{{ 'negative' if memory_used_change > 0 else 'positive' }}">
                            {{ '+' if memory_used_change > 0 else '' }}{{ memory_used_change }} GB
                        </td>
                    </tr>
                </tbody>
            </table>
        </div>

        <!-- Recommendations -->
        {% if recommendations and recommendations | length > 0 %}
        <div class="section">
            <h2 class="section-title">Recommandations</h2>
            <div class="recommendations">
                <h3>⚠️ Actions Recommandées</h3>
                <ul>
                    {% for recommendation in recommendations %}
                    <li>{{ recommendation }}</li>
                    {% endfor %}
                </ul>
            </div>
        </div>
        {% endif %}

        <!-- System Information -->
        <div class="section">
            <h2 class="section-title">Informations Système</h2>
            <div class="metrics-grid">
                <div class="metric-card">
                    <h3>Configuration</h3>
                    <div class="metric-row">
                        <span class="metric-label">OS</span>
                        <span class="metric-value">{{ os_version }}</span>
                    </div>
                    <div class="metric-row">
                        <span class="metric-label">CPU</span>
                        <span class="metric-value">{{ cpu_count }} vCPUs</span>
                    </div>
                    <div class="metric-row">
                        <span class="metric-label">Mémoire Totale</span>
                        <span class="metric-value">{{ memory_total }} GB</span>
                    </div>
                </div>
            </div>
        </div>

        <!-- Footer -->
        <div class="footer">
            <p>Rapport généré par Ansible for IBM i - Tech Sales France</p>
            <p>{{ report_date }}</p>
        </div>
    </div>
</body>
</html>